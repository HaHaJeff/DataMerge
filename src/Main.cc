#include "../src/Merge.h"

#include <iostream>

using namespace std;

int main(int argc, char** argv) {

//	if(argc != 4) printf("--fInPath(dir) fOutPath(file) match");
//	string fInPath = argv[1];
//	string fOutPath = argv[2];
//	string match = argv[3];
//	MergeFile(fInPath, fOutPath, match);
//	TestSendFile(argc, argv);
//	TestReadWrite(argc, argv);
	
//	vector<string> cmd;
//	ParseCommand(argc, argv, cmd);
	return 0;
}

