#ifndef MERGE_H
#define MERGE_H

/*
//64 means that the I's dimension of single block
int x_s = block_num/4 * 64 + block_num/4;
int x_e = x_s + 64;

//32 means that the J's dimension of single block
int y_s = block_num%2 * 32 + block_num%2;
int y_e = y_s + 32;

//16 means that the K's dimension of single block
int z_s = (block_num/2)%2 * 32 + (block_num/2)%2;
int z_e = z_s + 32;
*/

class CMerge {
public:
private:

};

#endif
